
<div class="w-100 bg-white rounded-lg shadow-lg flex flex-col h-[100vh]">
  <!-- Chat Header -->
  <div class="bg-blue-600 text-white p-4 rounded-t-lg">
    <h2 class="text-xl font-semibold">💬 Socket.IO Chat</h2>
  </div>
  <!-- Chat Messages -->
  <ul id="messages" class="flex-1 p-4 overflow-y-auto chat-container space-y-3">
    <!-- Messages will be dynamically added here -->
  </ul>
  <!-- Input Area -->
  <div class="p-4 border-t border-gray-200">
    <div class="flex space-x-2">
      <input id="msg" autocomplete="off" placeholder="Type a message..."
        class="flex-1 p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
      <button id="sendButton" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
        Send
      </button>
    </div>
  </div>
</div>

